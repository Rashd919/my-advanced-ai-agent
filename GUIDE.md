# دليل الوكيل الذكي المتقدم

## مقدمة

وكيل ذكي Python متقدم مستقل تماماً مع قدرات تنفيذ غير مقيدة وذاكرة مستمرة وواجهة Streamlit عربية احترافية.

## المميزات الرئيسية

### 1. 🧠 الذاكرة المستمرة والتعلم الذاتي
- **ChromaDB**: تخزين دائم للتفاعلات والدروس المستفادة
- **البحث الذكي**: البحث عن التفاعلات السابقة المشابهة
- **التعلم المستمر**: تحسين الأداء من خلال الخبرات السابقة

### 2. 🎯 التفكير المنطقي المتقدم
- **تقسيم المهام**: تحليل المهام المعقدة إلى خطوات بسيطة
- **تتبع مسار التفكير**: عرض مفصل لكل خطوة من خطوات التفكير
- **درجات الثقة**: قياس موثوقية كل قرار

### 3. 🛠️ أدوات متقدمة
- **مفسر Python**: تنفيذ كود Python مباشرة
- **تصفح الويب**: جمع المعلومات من الإنترنت
- **تحليل البيانات**: معالجة ملفات CSV و Excel
- **إدارة الملفات**: قراءة وكتابة الملفات

### 4. 💬 واجهة Streamlit التفاعلية
- **دعم عربي كامل**: واجهة مصممة للغة العربية
- **لوحة تحكم شاملة**: إحصائيات وسجلات مفصلة
- **عرض مسار التفكير**: رؤية كيفية اتخاذ القرارات

## البدء السريع

### 1. التثبيت

```bash
cd /home/ubuntu/ai_agent_advanced
pip install -r requirements.txt
```

### 2. إعداد مفتاح API

تأكد من أن ملف `.env` يحتوي على مفتاح OpenAI API الصحيح:

```bash
cat .env
```

يجب أن ترى:
```
OPENAI_API_KEY=sk-proj-...
AGENT_NAME=الوكيل الذكي المتقدم
AGENT_LANGUAGE=ar
```

### 3. تشغيل التطبيق

```bash
streamlit run ui/streamlit_app.py
```

سيفتح التطبيق على `http://localhost:8501`

## استخدام الوكيل

### المحادثة الأساسية

1. انتقل إلى قسم "💬 المحادثة"
2. اكتب طلبك في مربع الإدخال
3. اضغط "📤 إرسال"
4. انتظر رد الوكيل

### أمثلة على الطلبات

```
"اكتب برنامج يحسب مجموع الأرقام من 1 إلى 100"
"حلل ملف البيانات وأخبرني عن الاتجاهات"
"ابحث عن معلومات عن الذكاء الاصطناعي"
"اكتب دالة تحول النصوص إلى أحرف كبيرة"
```

### استخدام الأدوات المتقدمة

#### تنفيذ كود Python
1. اذهب إلى قسم "🛠️ الأدوات"
2. اختر "Python Code Executor"
3. اكتب الكود
4. اضغط "▶️ تنفيذ الكود"

مثال:
```python
import math
print(f"جذر 16 = {math.sqrt(16)}")
```

#### تصفح الويب
1. اختر "Web Scraper"
2. أدخل رابط الموقع
3. اضغط "🌐 تصفح الموقع"

#### تحليل البيانات
1. اختر "Data Analyzer"
2. أدخل مسار ملف CSV أو Excel
3. اضغط "📊 تحليل البيانات"

## فهم مسار التفكير

### قسم "🧠 التفكير المنطقي"

يعرض جميع خطوات التفكير:

- **🔍 تحليل**: فهم المشكلة
- **📋 تخطيط**: تخطيط الحل
- **⚙️ تنفيذ**: تنفيذ الخطوات
- **✅ تقييم**: التحقق من النتائج
- **📚 تعلم**: استخراج الدروس

كل فكرة تحتوي على:
- **المحتوى**: ما تم التفكير فيه
- **التبرير**: لماذا تم اتخاذ هذا القرار
- **درجة الثقة**: مدى ثقة الوكيل (0-100%)

## نظام الذاكرة

### التفاعلات السابقة

يتم حفظ جميع المحادثات السابقة:
- تاريخ التفاعل
- المدخل والرد
- البيانات الوصفية

### الدروس المستفادة

يمكنك إضافة دروس مستفادة:

1. اذهب إلى قسم "📚 الذاكرة"
2. اختر "الدروس"
3. أدخل الدرس والفئة والأهمية
4. اضغط "💾 حفظ الدرس"

الدروس المهمة (أهمية ≥ 7) يتم استرجاعها تلقائياً عند الحاجة.

## الإعدادات والإجراءات

### الشريط الجانبي

يحتوي على:

- **معلومات الجلسة**: معرف الجلسة وعدد الأدوار
- **إحصائيات الذاكرة**: عدد التفاعلات والدروس
- **الأدوات المتاحة**: قائمة الأدوات المثبتة
- **الإجراءات**:
  - 🔄 إعادة تعيين الجلسة
  - 💾 تصدير الجلسة

## الملفات المهمة

```
ai_agent_advanced/
├── core/
│   ├── agent.py          # محرك الوكيل الرئيسي
│   ├── memory.py         # نظام الذاكرة
│   ├── reasoning.py      # محرك التفكير المنطقي
│   └── tools.py          # الأدوات المتقدمة
├── ui/
│   └── streamlit_app.py  # واجهة Streamlit
├── data/
│   ├── chroma_db/        # قاعدة بيانات ChromaDB
│   ├── memory.json       # ملف الذاكرة
│   └── logs/             # السجلات
├── .env                  # متغيرات البيئة
├── requirements.txt      # المكتبات المطلوبة
└── test_agent.py         # اختبارات
```

## استكشاف الأخطاء

### خطأ: "OPENAI_API_KEY غير موجود"

**الحل**: تأكد من أن ملف `.env` يحتوي على مفتاح API صحيح:

```bash
echo "OPENAI_API_KEY=your_key_here" >> .env
```

### خطأ: "Invalid or expired sandbox token"

**الحل**: مفتاح API قد يكون منتهي الصلاحية. احصل على مفتاح جديد من OpenAI.

### الوكيل بطيء جداً

**الحل**: قلل حجم الذاكرة أو امسح التفاعلات القديمة:

```python
memory.clear_old_interactions(days=7)
```

## الأمان

### نصائح الأمان

1. **لا تشارك مفتاح API**: احفظه في `.env` فقط
2. **استخدم `.gitignore`**: أضف `.env` إلى `.gitignore`
3. **تحديث المكتبات**: حافظ على المكتبات محدثة

```bash
pip install --upgrade -r requirements.txt
```

## التطوير والتوسع

### إضافة أداة جديدة

```python
from core.tools import Tool

class MyTool(Tool):
    def __init__(self):
        super().__init__(
            name="My Tool",
            description="وصف الأداة"
        )
    
    def execute(self, **kwargs):
        # تنفيذ الأداة
        return {"success": True, "result": "..."}

# تسجيل الأداة
toolbox.register_tool(MyTool())
```

### تخصيص تعليمات النظام

عدّل `_build_system_prompt()` في `core/agent.py`:

```python
def _build_system_prompt(self) -> str:
    return """أنت وكيل ذكي متخصص في...
    """
```

## الدعم والمساعدة

### الموارد المفيدة

- [OpenAI API Documentation](https://platform.openai.com/docs)
- [Streamlit Documentation](https://docs.streamlit.io)
- [ChromaDB Documentation](https://docs.trychroma.com)

### الإبلاغ عن المشاكل

إذا واجهت مشكلة:

1. تحقق من السجلات في `data/logs/`
2. جرب إعادة تعيين الجلسة
3. تأكد من تثبيت جميع المكتبات

## الخلاصة

الوكيل الذكي المتقدم هو أداة قوية ومرنة للقيام بمهام معقدة. استخدمه بحكمة واستمتع بقوة الذكاء الاصطناعي!

---

**الإصدار**: 1.0.0  
**آخر تحديث**: فبراير 2026  
**اللغة**: العربية
